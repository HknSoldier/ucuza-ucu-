name: SNIPER V60 - WORLD DOMINATION

on:
  schedule:
    # Her 4 saatte bir çalış (Günde 6 kez)
    - cron: '0 */4 * * *'
  workflow_dispatch:

concurrency:
  group: sniper-world
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  world-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    
    steps:
      - name: 🚀 Depoyu Çek
        uses: actions/checkout@v4
      
      - name: 🐍 Python 3.11 Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Kütüphaneleri Yükle
        run: pip install requests fast-flights beautifulsoup4 pandas python-dotenv
      
      - name: 🎯 Dünya Taraması Başlat
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          PYTHONUNBUFFERED: 1
        run: python main.py

      - name: 💾 Hafızayı Kaydet (scan_state.json)
        if: always()
        run: |
          git config --global user.name "SNIPER AI"
          git config --global user.email "bot@sniper.com"
          git add scan_state.json sent_deals.txt
          git commit -m "💾 Hafıza Güncellendi: $(date)" || echo "Değişiklik yok"
          git push
