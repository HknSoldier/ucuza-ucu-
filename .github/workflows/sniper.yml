name: SNIPER V50 - HOLIDAY PACKAGER

on:
  schedule:
    # UTC: 06, 12, 18 -> Türkiye: 09:00, 15:00, 21:00
    - cron: '0 6,12,18 * * *'
  
  workflow_dispatch: # Manuel test butonu

concurrency:
  group: sniper-holiday
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  holiday-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    
    steps:
      - name: 🚀 Depoyu Çek
        uses: actions/checkout@v4
      
      - name: 🐍 Python 3.11 Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Kütüphaneleri Yükle
        run: |
          pip install requests fast-flights beautifulsoup4 pandas python-dotenv
      
      - name: 🎯 Tatil Taramasını Başlat
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          PYTHONUNBUFFERED: 1
        run: python main.py

      - name: 💾 Verileri Kaydet
        if: always()
        run: |
          git config --global user.name "SNIPER AI"
          git config --global user.email "bot@sniper.com"
          git add sent_deals.txt
          git commit -m "💾 Veri Güncellemesi" || echo "Değişiklik yok"
          git push
