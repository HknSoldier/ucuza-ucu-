name: SNIPER V50 - HOLIDAY PACKAGER

on:
  schedule:
    # UTC Saatleri: 06:00, 12:00, 18:00 (Türkiye saati +3: 09:00, 15:00, 21:00)
    - cron: '0 6,12,18 * * *'
  
  workflow_dispatch: # Manuel tetikleme butonu (Test için)

concurrency:
  group: sniper-holiday-scan
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  sniper-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 350 # Uzun taramalar için süre artırıldı
    
    steps:
      - name: 🚀 Depoyu Çek
        uses: actions/checkout@v4
      
      - name: 🐍 Python 3.11 Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests fast-flights beautifulsoup4 pandas python-dotenv
      
      - name: 🕵️ Anti-Fingerprint Ayarları
        run: |
          export TZ="Europe/Istanbul"
          echo "🌍 Sistem Saati: $(date)"
      
      - name: 🎯 Taramayı Başlat
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          PYTHONUNBUFFERED: 1
        run: python main.py

      - name: 💾 Hafızayı Kaydet (Git Commit)
        if: always()
        run: |
          git config --global user.name "SNIPER AI"
          git config --global user.email "sniper-bot@actions.github.com"
          git add sent_deals.txt flight_analytics.csv
          git commit -m "💾 Veri Güncellemesi: $(date)" || echo "Değişiklik yok"
          git push
