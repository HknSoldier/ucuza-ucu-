name: SNIPER V70 - AUTONOMOUS

on:
  schedule:
    # Her 6 saatte bir (Günde 4 kez yeterli)
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  intel-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    
    steps:
      - name: 🚀 İstihbarat Merkezini Kur
        uses: actions/checkout@v4
      
      - name: 🐍 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Araçları Yükle
        run: pip install -r requirements.txt
      
      - name: 🛰️ Sinyal Taraması & Analiz
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          PYTHONUNBUFFERED: 1
        run: python main.py

      - name: 💾 Hafızayı Güncelle (Git Commit)
        if: always()
        run: |
          git config --global user.name "SNIPER AI"
          git config --global user.email "intel@sniper.com"
          git add price_history.json
          git commit -m "🧠 Hafıza Güncellendi" || echo "Değişiklik yok"
          git push
