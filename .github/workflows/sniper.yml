name: SNIPER V80 - AUTONOMOUS

on:
  schedule:
    # Her 6 saatte bir çalışır
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    
    steps:
      - name: 🚀 Başlat
        uses: actions/checkout@v4
      
      - name: 🐍 Python Hazırla
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Paketleri Kur
        run: pip install -r requirements.txt
      
      - name: 🛰️ Taramayı Ateşle
        env:
          PYTHONUNBUFFERED: 1
        run: python main.py

      - name: 💾 Hafızayı Kaydet
        if: always()
        run: |
          git config --global user.name "SNIPER AI"
          git config --global user.email "intel@sniper.com"
          git add price_history.json
          git commit -m "🧠 Hafıza Güncellendi" || echo "Değişiklik yok"
          git push
